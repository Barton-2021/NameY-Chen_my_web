{
  "name": "next",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "set NODE_OPTIONS=--openssl-legacy-provider && next dev -H localhost -p 3000",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && next build && next export",
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && next start",
    "lint": "next lint"
  },
  "dependencies": {
    "antd": "^5.27.1",
    "next": "11.1.2",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "eslint": "^8.1.0",
    "eslint-config-next": "11.1.2"
  }
}
